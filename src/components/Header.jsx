import PropTypes from 'prop-types';
import './Header.css';

const Header = () => {
    return (
        <header>
            <h1 className="header-text">Fake Store</h1>
        </header>
    );
};

Header.propTypes = {};

export default Header;
